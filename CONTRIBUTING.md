# Contributing

GAL Labs is currently in active alpha development.

For now:
- open an issue for defects or proposed changes
- describe the evidence or test case that exposed the problem
- reference the relevant GAL control when possible
- avoid mixing product decisions with implementation changes in the same change set

The project currently uses regression-driven hardening, with ExpPay as the baseline scenario and named regression controls for specific failure classes.
