# GAL Test Suite v0.5.0

Keep this directory outside the runtime project during blind host testing.

It contains:
- baseline scenarios
- regression controls
- expected behavior

The runtime package must not be able to read these files during a blind test.
